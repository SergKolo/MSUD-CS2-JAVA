/****************************************
Author: Sergiy Kolodyazhnyy
Course: CS 2050
Date: Sept 21 2015
Instructor: Prof Gurka
Java version: OpenJDK, 1.7.0
IDE: nano text-editor and java compiler
Project: #4, Big Addition, preliminary
*****************************************/

import java.io.File;
import java.util.Scanner;
import java.io.IOException;

public class driver
{
    public static void main(String[] args) throws IOException
    {
        String planAuthor;
        File inpFile = new File(args[0]);
        Scanner readData = new Scanner(inpFile);
        data[] inputData =  new data[5];

	printHeader();
        for(int i=0; i<5; i++)
            inputData[i] = new data();

        for(int i=0; i<5; i++)
            {
                if (i == 0)
                    planAuthor = readData.nextLine();

                inputData[i].caseDescription = readData.nextLine();
                inputData[i].opA = readData.nextLine();
                inputData[i].opB = readData.nextLine();
                inputData[i].result = readData.nextLine();
                //inputData[i].performAddition();
                //System.out.println("***");

            }

        /*for (int i = 0; i<5;i++)
        {
        	int size = inputData[i].op1.length();
        	for (int j = 0; j<size; j++)
        }*/


        for (int i=0; i<5; i++)
            {

                System.out.println("Case:" + inputData[i].caseDescription);
                System.out.println("Operand 1: " + inputData[i].opA);
                System.out.println("Operand 2: " + inputData[i].opB);
                System.out.println("Expected Result: " + inputData[i].result);
                System.out.print("Result from stack arithmetic: ");
                inputData[i].performAddition();
                System.out.printf("\n\n");
            }

    }
    public static void printHeader()
    {
	System.out.println("Author: Sergiy Kolodyazhnyy\tCS-2050,Fall-2015");
	System.out.println("Project #4 - Big Addition\n\n");
    }

}
